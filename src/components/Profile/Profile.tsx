import React from 'react';

import useStyles from './Profile.styles';

const Profile = () => {
  const classes = useStyles();

  return <div className={classes.container}>Profile</div>;
};

export default Profile;
